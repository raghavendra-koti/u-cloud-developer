language: minimal

services: docker

env:
  #- DOCKER_COMPOSE_VERSION=1.23.2

before_install:
  - docker -v 
  #&& docker-compose -v
  #- sudo rm /usr/local/bin/docker-compose
  #- curl -L https://github.com/docker/compose/releases/download/${DOCKER_COMPOSE_VERSION}/docker-compose-`uname -s`-`uname -m` > docker-compose
  #- chmod +x docker-compose
  #- sudo mv docker-compose /usr/local/bin

install:
  #- docker-compose -f course-03/exercises/udacity-c3-deployment/docker/docker-compose-build.yaml build --parallel
  # - docker login -u "$DOCKER_USER" -p "$DOCKER_PASSWORD"
  
  # - docker build -t udacity-restapi-feed course-03/exercises/udacity-c3-restapi-feed/.
  # - docker tag udacity-restapi-feed shaastra/udacity-restapi-feed:prod 
  # - docker push shaastra/udacity-restapi-feed:prod

  # - docker build -t udacity-restapi-user course-03/exercises/udacity-c3-restapi-user/.
  # - docker tag udacity-restapi-user shaastra/udacity-restapi-user:prod 
  # - docker push shaastra/udacity-restapi-user:prod

  # - docker build -t udacity-frontend course-03/exercises/udacity-c3-frontend/.
  # - docker tag udacity-frontend shaastra/udacity-frontend:prod 
  # - docker push shaastra/udacity-frontend:prod

  # - docker build -t reverseproxy course-03/exercises/udacity-c3-deployment/docker/.
  # - docker tag reverseproxy shaastra/reverseproxy:prod 
  # - docker push shaastra/reverseproxy:prod
  - curl -LO https://storage.googleapis.com/kubernetes-release/release/v1.14.0/bin/linux/amd64/kubectl
  - chmod +x ./kubectl
  - sudo mv ./kubectl /usr/local/bin/kubectl
  - sudo mkdir ~/.kube

  - curl -o aws-iam-authenticator https://amazon-eks.s3-us-west-2.amazonaws.com/1.14.6/2019-08-22/bin/linux/amd64/aws-iam-authenticator
  - chmod +x ./aws-iam-authenticator
  - sudo mv ./aws-iam-authenticator /usr/local/bin/aws-iam-authenticator
  #- sudo kubeadm init --pod-network-cidr=10.244.0.0/16
  #- sudo sysctl net.bridge.bridge-nf-call-iptables=1
  - sudo mv kubeconfig $HOME/.kube/config
  - sudo chown $(id -u):$(id -g) $HOME/.kube/config
  - sudo chown $(id -u):$(id -g) $HOME/.kube/
  #- sudo kubectl config set clusters.cluster.certificate-authority-data $KUBE_CERTIFICATE
  #- sudo kubectl config set clusters.cluster.server $KUBE_SERVER
  - kubectl config view --raw
  - sudo kubectl apply -f course-03/exercises/udacity-c3-deployment/k8s/backend-feed-deployment.yaml




